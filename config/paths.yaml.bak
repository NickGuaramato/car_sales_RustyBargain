
  #Results and statistics and dirs
  dirs:
    raw: "data/raw/"
    processed: "data/processed/"
    artifacts: "artifacts/"
    metrics: "artifacts/reports/"
    plots: "artifacts/plots/"
    logs: "artifacts/logs/"
    selected_models: "artifacts/models/"
  #Data
  files:
    raw_data: "${dirs.raw}/car_data.csv"
    processed_data: "${dirs.processed}/data_processed.parquet"
    final_data: "${dirs.processed}/final_data.parquet"
